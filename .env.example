# =============================================================================
# Fantasy NBA — 环境变量模板
# 复制为 .env 并根据环境复制 .env.development 或 .env.production
# =============================================================================

# 运行环境：development | production
# 影响：synchronize（生产环境关闭自动建表）
NODE_ENV=development

# -----------------------------------------------------------------------------
# Database（开发/生产使用不同库名隔离）
# -----------------------------------------------------------------------------
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=password
# 开发：fantasy_nba | 生产：fantasy_nba_prod（或通过 .env.production 覆盖）
DB_DATABASE=fantasy_nba

# 可选：开启 SQL 日志，调试用
# DB_LOGGING=false

# -----------------------------------------------------------------------------
# JWT
# -----------------------------------------------------------------------------
JWT_SECRET=supersecretkey_change_in_production
JWT_EXPIRES_IN=7d

# -----------------------------------------------------------------------------
# Ports（合并后单端口）
# -----------------------------------------------------------------------------
API_PORT=3001

# -----------------------------------------------------------------------------
# Scraper（可选，新浪体育同步）
# SINA_BASE_URL=https://slamdunk.sports.sina.com.cn/api
# NBA_SEASON=2025
# POLL_INTERVAL_MINUTES=5
# GAME_START_HOUR=8
# GAME_END_HOUR=14
